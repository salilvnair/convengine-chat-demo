# Next export base path used in static HTML asset URLs.
# For github.io dist hosting, this should usually be /dist/demos/chat
NEXT_BASE_PATH=/framework/convengine-chat

# Path from this repo to your github.io repo root.
GITHUB_IO_REPO_PATH=../../salilvnair.github.io

# Target folder inside github.io repo where /out is synced.
GITHUB_IO_SYNC_SUBDIR=public/framework/convengine-chat
